# breakpoint
breakpoint is open for open source contributions

## Open Source Guide:
In the age of unoptimzed program developing, breakpoint rather looks at optimization that users would like. Thus, the design choices made in `/crates/bp-core` (still in development)
- `event.rs`